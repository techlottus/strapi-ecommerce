'use strict';

/**
 * track-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::track-subscription.track-subscription');
