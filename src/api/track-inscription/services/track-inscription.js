'use strict';

/**
 * track-inscription service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::track-inscription.track-inscription');
